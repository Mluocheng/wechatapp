<view class="container" style="padding-top:{{statusBarHeight}}px">
  <view class="header">
    <view class='title'>
      查看推送
    </view>
  </view>
</view>
<view style='margin-top:{{statusBarHeight + 60}}px'></view>

<view wx:if="{{btn_show}}" class="section box" wx:for="{{projectlistdata.data.onlyRegion}}" wx:key="{{item.pushTime}}">
  <view class="section_data">
    {{item.pushTime}}
  </view>
  <view class="flex-wrp">
    <view bindtap='projectlistclick' class="flex-item bgcolor" wx:for="{{item.pushList}}" wx:key="{{item.id}}" data-articleid="{{item.id}}">
      <view class='itmeandmoney'>
        <view class='left_time lf'>
          报名截止时间:
          <text class='timemoneycolcor'>{{item.applyEndDate}}</text>
        </view>
        <view class='rightmoner rf'>
          项目金额：
          <text class='timemoneycolcor'>{{item.projectFunds}}万</text>
        </view>
      </view>
      <view class='clearfloat'></view>
      <view class='item_conter'>
        {{item.title}}
      </view>
      <view class='requirement'>
        <view>
          <text>资质要求</text> {{item.quaRequirement}}</view>
      </view>
    </view>
  </view>
</view>

<view class='nothing' wx:if="{{!btn_show}}">
  <view>
    <image src='../../images/request_error_1@2x.png'></image>
  </view>
  <view>
    这里还什么都没有哦
  </view>
  <view>
  先登录试试！
  </view>
</view>

<view class='nothing' wx:if="{{btn_show && projectlistdata.data.onlyRegion==0}}">
  <view>
    <image src='../../images/request_error_1@2x.png'></image>
  </view>
  <view>
    暂时没有数据！
  </view>
</view>